# Phase 1 — ingestion + storage
httpx>=0.27          # async HTTP client for HN fan-out
# PostgREST via httpx — no direct Postgres driver needed
python-dotenv>=1.0   # load .env locally

# Phase 2 — RSS sources + full-text extraction
feedparser>=6.0      # RSS/Atom feed parsing
trafilatura>=1.6     # full-text extraction from web pages

# Phase 3 — embeddings + chunking + retrieval
sentence-transformers>=2.7   # BGE-base embeddings (CPU)

# Phase 4 — LLM synthesis
huggingface-hub>=0.23        # InferenceClient for Llama via HF Inference API

# Phase 6 — FastAPI web service
fastapi>=0.111               # API framework for digest browse + chat Q&A
uvicorn[standard]>=0.30      # ASGI server